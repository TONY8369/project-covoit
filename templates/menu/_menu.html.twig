
<nav class="navbar navbar-expand-lg bg-primary data-bs-theme="dark"">
  <div class="container-fluid">
    {# <a class="navbar-brand text-light" href="{{ path('article_date')}}">Navbar</a> #}
    <button class="navbar-toggler text-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon text-light"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_home')}}">Acceuil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link " href="{{ path('app_association_new') }}">Create association</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled">Contact</a>
        </li>
      </ul>

    </div>
      <div class="d-flex justify-content-end">
            <a href="{{ path('app_user_new')}}" class="btn btn-info">Sign up</a>
            <a href="{{ path('app_login') }}" class="btn btn-warning">Sign in</a>
            <a href="{{ path('app_logout') }}" class="btn btn-danger">Logout</a>
      </div>

  </div>
</nav>