{% extends "base.html.twig" %}

{% block title %}Nouveau groupe{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4">Création d'un nouveau groupe</h1>
        {# {{ form(form)}} #}
        {{ include('group/_form.html.twig')}}
    </div>
    <a href="{{path('group_index')}}">Retour à la liste des groupes</a>
{% endblock %}