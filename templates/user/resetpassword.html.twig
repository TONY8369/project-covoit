{# {% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<form method="post">
    <h1> Réinitialisez votre mot de passe ✅</h1> #}
    {# {{ form_start(requestPassForm) }} #}
    {# <div class="d-flex justify-content-center">{{ form_errors(requestPassForm) }}</div> #}

    {# {{ form_label(requestPassForm.email, null, {'label_attr': {'class':'form-label'}} ) }} 
     {{ form_widget(requestPassForm.email, {'attr': {'class':'form-control'}}) }}
     {{ form_row(requestPassForm._token) }} #}
{# <div class="d-flex flex-column mt-3">
    {{ form_label(requestPassForm.password, null, {'label_attr': {'class':'form-label'}} ) }}
    {{ form_widget(requestPassForm.password, {'attr': {'class':'form-control'}}) }}
</div> #}

    {# <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    
    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}">

     <button class="btn btn-primary mt-2" type="submit">
        Submit
    </button> #}
    {# {{ form_end(requestPassForm)}} #}
    {# </div>  #}

    {# <a type="button" class="btn btn-secondary mt-2" href="{{ path('login') }}">Sign up</a> #}
{# </form>
{% endblock %} #}
